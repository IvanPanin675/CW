export const Telecommunications = () => {
    return (
        <main className="mainTel">
            <h1>Telecommunications</h1>
            <p>network cable laying, network configuration, skills in optical cable welding, switch configuration, IP telephony server setup based on Linux CentOS using Asterisk.</p>
        </main>
    )
}